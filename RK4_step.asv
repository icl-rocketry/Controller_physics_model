function [y_, t_] = RK4_step(t, y, dt)
K1 = Dynamics_wrapper(t, y);
K2 = Dynamics_wrapper(t + dt / 2, y + dt .* K1 / 2);
K3 = Get_state_derivative(t + dt / 2, y + dt .* K2 / 2);
K4 = Get_state_derivative(t + dt, y + dt .* K3);

y_ = y + (dt / 6) .* (K1 + 2 * K2 + 2 * K3 + K4);
q = y_(7:10); 
y_(7:10) = q / norm(q);
t_ = t + dt;
end